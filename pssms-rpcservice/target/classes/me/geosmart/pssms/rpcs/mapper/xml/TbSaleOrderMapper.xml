<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.geosmart.pssms.rpcs.mapper.TbSaleOrderMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="me.geosmart.pssms.rpcs.entity.TbSaleOrder">
        <id column="serialId" property="serialId"/>
        <result column="sale_order_id" property="saleOrderId"/>
        <result column="order_date" property="orderDate"/>
        <result column="customer_code" property="customerCode"/>
        <result column="product_code" property="productCode"/>
        <result column="number" property="number"/>
        <result column="price" property="price"/>
        <result column="amount" property="amount"/>
        <result column="memo" property="memo"/>
    </resultMap>

    <select id="groupByProduct" resultType="java.util.HashMap">
        SELECT product_code,sum(amount) as amount  FROM tb_sale_order
        WHERE amount is not null
        GROUP BY product_code
        ORDER BY product_code
    </select>

</mapper>
